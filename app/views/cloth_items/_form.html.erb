<%= form_for(@cloth_item) do |f| %>
  <% if @cloth_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cloth_item.errors.count, "error") %> prohibited this cloth_item from being saved:</h2>

      <ul>
      <% @cloth_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :no %><br />
    <%= f.number_field :no %>
  </div>
  <div class="field">
    <%= f.label :unit_price %><br />
    <%= f.number_field :unit_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
