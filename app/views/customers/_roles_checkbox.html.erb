
<li><%= check_box_tag "user[role_ids][]",
					 current_role.id, 
					 @user.roles.include?(current_role), 
					 :disabled =>false %>
					 <%= current_role.name %></li>
					<%= the_children %>
				